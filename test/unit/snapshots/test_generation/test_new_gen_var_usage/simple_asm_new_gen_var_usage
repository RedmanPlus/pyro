section .text
global _start

_start:
    mov rax, 34
    mov rbx, 35
    add rax, rbx
    push rax
    mov rax, 5
    mov rbx, 7
    imul rax, rbx
    mov rbx, 10
    imul rax, rbx
    mov rbx, QWORD [rsp + 0]
    add rax, rbx
    mov rbx, 1
    add rax, rbx
    push rax
    mov rax, 60
    mov rdi, 0
    syscall