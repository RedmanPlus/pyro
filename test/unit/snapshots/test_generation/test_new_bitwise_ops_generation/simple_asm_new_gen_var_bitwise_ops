section .text
global _start

_start:
    mov rdx, 0
    mov rax, 5
    mov rbx, 6
    mul rbx
    mov r8, rax
    mov r9, 1
    sub r8, r9
    mov r10, 1
    not r10
    mov r9, 3
    mov r10, 4
    add r9, r10
    mov rdx, 0
    mov rax, 2
    mov rbx, r10
    div r10
    mov r10, rax
    mov r9, 2
    and r8, r9
    xor r9, r10
    or r8, r9
    push r8
    add rsp, 8
    mov rax, 60
    mov rdi, 0
    syscall