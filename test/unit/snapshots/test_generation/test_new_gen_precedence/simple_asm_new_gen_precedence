section .text
global _start

_start:
    mov rax, 2
    mov rbx, 3
    imul rax, rbx
    mov rbx, 1
    add rax, rbx
    mov rbx, 4
    mov rcx, 5
    imul rbx, rcx
    sub rax, rbx
    push rax
    mov rax, 60
    mov rdi, 0
    syscall
