section .text
global _start

_start:
    mov rax, 34
    mov rbx, 35
    add rax, rbx
    push rax
    mov rax, 150
    mov rbx, 150
    add rax, rbx
    mov rbx, 20
    add rax, rbx
    push rax
    mov rax, 60
    mov rdi, 0
    syscall